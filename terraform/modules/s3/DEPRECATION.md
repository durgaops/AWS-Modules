# LEGACY SHIM — prefer terraform/modules/storage/s3-bucket

This folder is a thin wrapper around `modules/storage/s3-bucket`.
Use `modules/storage/s3-bucket` or `compositions/storage/secure-storage`.
